# ui-quality-gate

## 触发

凡是修改 UI、样式、组件、页面时必须触发。

## 步骤

1. 读取 `docs/UI_DESIGN_SYSTEM.md`。
2. 读取 `docs/UI_REFERENCE_RESEARCH.md`。
3. 检查字体、间距、表单、中文文案、按钮、状态、响应式、时间格、候选人隐私显示。
4. 必要时运行 `scripts/ui-snapshots.sh`。

## 禁止

不得接受“能用但丑”的界面。不得使用默认丑表单、拥挤布局、廉价渐变。
