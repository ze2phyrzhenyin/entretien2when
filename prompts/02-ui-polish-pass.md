# 02 UI Polish Pass

触发 `ui-quality-gate`。读取 `docs/UI_DESIGN_SYSTEM.md` 和 `docs/UI_REFERENCE_RESEARCH.md`，检查页面：

- 字体和中文显示。
- 间距、表单、按钮、状态。
- 时间格。
- 响应式。
- loading/empty/error/success。

运行 `scripts/ui-snapshots.sh` 或记录无法运行原因。
